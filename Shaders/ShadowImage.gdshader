shader_type canvas_item;
#include "Includes/ShadowEffect.gdshaderinc"

uniform float shade:hint_range(0,1)=0.2;
uniform float shadeAlpha:hint_range(0,1)=0.8;

void fragment() {
	COLOR=AddShadow(COLOR, UV, shade, shadeAlpha);
}
