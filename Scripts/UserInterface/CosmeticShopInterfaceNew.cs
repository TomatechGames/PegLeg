using Godot;

public partial class CosmeticShopInterfaceNew : Control
{
    [Export]
    RecycleListContainer compactCosmeticList;

    // get cosmetic offers from new system
    // apply filters to cosmetics
    // group by cosmetics layoutId
    // build list of layouts, filtered to the layoutIds in the grouped cosmetics
    // populate layout pages and headers as they scroll onscreen
    // estimated 12-16 offers needed at a time, compared to the dozens of dozens that are currently spawned


}
